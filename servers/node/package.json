{
  "name": "sqs-server",
  "version": "1.0.1",
  "description": "A tiny little queue system that supports a single consumer client to be connected",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sqs-server": "./dist/sqs-server"
  },
  "scripts": {
    "build": "tsc",
    "compileAndWatch": "tsc -w",
    "prepack": "cp src/sqs-server dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist/**/*"
  ],
  "preferGlobal": true,
  "license": "MIT",
  "dependencies": {
    "@types/uuid": "^3.4.5",
    "rxjs": "^6.5.2",
    "sqlite": "^3.0.3",
    "typescript": "^3.6.4",
    "uuid": "^3.3.3"
  },
  "author": "Kevin Guanche Darias",
  "repository": {
    "type": "git",
    "url": "git+https://https://github.com/KevinGuancheDarias/sqs.git"
  },
  "keywords": [
    "sqs",
    "simple",
    "queue",
    "system"
  ],
  "devDependencies": {
    "@types/node": "^12.7.12"
  }
}